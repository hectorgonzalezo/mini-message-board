extends layout

block content
  h1  Create a message
  form(action="/new", method="post")
    label(for="name") Name
      input(type="text" name="name")
    label(for="text") Text
      input(type="text" name="text")
    input(type="submit")